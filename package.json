{
  "name": "fivefold-arc-pod-counter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm --prefix server start",
    "test": "npm --prefix server test"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
